/** 
* the type script follows the same rule as java sccript for variable declaration,they can be declared by using var let and cons
*/
function display(a:number,b:number)
{
	return a + b;
}
var s:number = display(100,15);
console.log('Sum of the two numbers is:'+s);